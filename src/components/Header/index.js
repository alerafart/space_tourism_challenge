// == Import component

// == Import style
import './header.scss';
// == Component
function Header() {
  
  return (
    <header className="header">
        header component
        <nav>
          {/* <Link to="/about">About</Link> */}
        </nav>
    </header>
  );
}

export default Header;